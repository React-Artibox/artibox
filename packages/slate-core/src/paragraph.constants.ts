/**
 * Provide the constant of type of `paragraph` since it is not customizable in `slate`.
 */
export const PARAGRAPH_TYPE = 'paragraph' as const;
export type PARAGRAPH_TYPE = typeof PARAGRAPH_TYPE;
